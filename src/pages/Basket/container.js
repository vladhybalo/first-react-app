import {getImages} from 'logic/cardContainer/actions'

export function mapStateToProps(state, ownProps) {
    const { busketProducts } = state
    return {busketProducts};
}

export function mapDispatchToProps(dispatch, ownProps) {
    return {

    }
};